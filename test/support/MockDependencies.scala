package com.originate.support

import com.originate.util.Tap._

import org.mockito.ArgumentMatchers._
import org.mockito.Matchers._
import org.mockito.Mockito._

trait MockDependencies { self: MockHelpers =>
}
